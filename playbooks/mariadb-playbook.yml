---
- name: Instalacion y configuración de mariadb server
  hosts: linux
  remote_user: ansible
  become: true
  tasks:
  
    - name: Upgrade repositorios
      ansible.builtin.include_tasks: ../modulos/mariadb/actualizarPaquetes.yml
  
    - name: Instalar MariaDB
      ansible.builtin.include_tasks: ../modulos/mariadb/instalarMariaDB.yml

    - name: Configurar servicio mariadb
      ansible.builtin.include_tasks: ../modulos/mariadb/configurarServicioMariaDB.yml

    - name: Configurar mariadb
      ansible.builtin.include_tasks: ../modulos/mariadb/configurarMariaDB.yml